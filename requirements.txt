# =========================================================
# متطلبات مشروع تشخيص اعتلال الكلى السكري عبر اللعاب
# Salivary Diabetic Nephropathy Diagnostic Assistant
# =========================================================

# --- واجهة التطبيق ---
streamlit>=1.38.0

# --- قواعد البيانات المحلية (ذاكرة دائمة) ---
chromadb>=0.5.5          # قاعدة بيانات متجهية محلية (Vector DB)
# sqlite3 مدمجة أصلاً في بايثون، لا تحتاج تثبيت

# --- الذكاء الاصطناعي المحلي (Offline Mode) ---
ollama>=0.3.3             # للتواصل مع نموذج Llama3.2-Vision محلياً (اختياري - قد لا يتوفر على السحابة)

# --- ملاحظة: البديل السحابي المجاني (Groq) ---
# لا يحتاج مكتبة إضافية، يعمل عبر urllib المدمجة في بايثون.
# فقط أضف مفتاح GROQ_API_KEY في Secrets عند النشر على Streamlit Cloud.

# --- البحث في الويب (Online Mode) ---
duckduckgo-search>=6.2.13

# --- معالجة الصور والبيانات ---
Pillow>=10.4.0
opencv-python-headless>=4.10.0.84
numpy>=1.26.4
pandas>=2.2.2
openpyxl>=3.1.5           # لقراءة ملفات إكسل

# --- الإحصاء والرسوم البيانية ---
scipy>=1.13.1
plotly>=5.23.0

# --- المجسمات ثلاثية الأبعاد ---
# ملاحظة: streamlit-threejs/pythreejs غير مستقرة دائماً في بيئات حديثة،
# لذلك الكود يعتمد أساساً على Plotly 3D Mesh (الأكثر استقراراً) 
# مع دعم اختياري لتصدير ملفات OBJ.

# --- إدارة الذاكرة والأداء ---
psutil>=6.0.0

# --- التعلم الآلي (نموذج تصنيف حقيقي مدرَّب على العينات) ---
scikit-learn>=1.5.1
joblib>=1.4.2

# --- تصدير تقارير PDF ---
fpdf2>=2.7.9

# =========================================================
# أوامر التثبيت (Windows / Mac / Linux):
#
#   1) إنشاء بيئة افتراضية:
#      python -m venv venv
#      source venv/bin/activate      (Mac/Linux)
#      venv\Scripts\activate         (Windows)
#
#   2) تثبيت المكتبات:
#      pip install -r requirements.txt
#
#   3) تثبيت Ollama (للنمط الأوفلاين):
#      حمّل من https://ollama.com/download
#      ثم في الطرفية:
#      ollama pull llama3.2-vision
#
#   4) تشغيل التطبيق:
#      streamlit run app.py
# =========================================================
google-genai
